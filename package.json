{
  "name": "mynana",
  "version": "1.0.0",
  "private": true,
  "description": "My Nana — scripts del servidor",
  "scripts": {
    "check-reminders": "node scripts/check-reminders.js"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "web-push": "^3.6.7",
    "@netlify/functions": "^2.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
